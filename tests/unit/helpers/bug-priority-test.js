
import { bugPriority } from 'btms/helpers/bug-priority';
import { module, test } from 'qunit';

module('Unit | Helper | bug priority');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bugPriority([42]);
  assert.ok(result);
});

