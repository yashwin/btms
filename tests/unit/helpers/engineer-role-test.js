
import { engineerRole } from 'btms/helpers/engineer-role';
import { module, test } from 'qunit';

module('Unit | Helper | engineer role');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = engineerRole([42]);
  assert.ok(result);
});

